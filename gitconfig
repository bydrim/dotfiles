[includeIf "hasconfig:remote.*.url:git@github.**/**"]
    path = ~/.github.gitconfig

[includeIf "hasconfig:remote.*.url:git@codeberg.**/**"]
    path = ~/.codeberg.gitconfig

[init]
    defaultBranch = main

[core]
    editor = nvim
